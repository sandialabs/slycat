.. _models:

Models
======

From an end-user perspective, the main functions of the Slycat Web Server are
creation and storage of models - where a *model* is a typed collection of
*artifacts* and metadata.  Slycat defines several specific model types, plus an
interactive visual user interface for each type.  The following documents each
model type in detail:

.. toctree::
   :maxdepth: 1

   parameter-image.rst
