***************************
** Use at your own risk. **
***************************

Only tested with Ubuntu 14.04 fresh install.  

Usage:
Run `bash setup.sh` from this directory.

It needs sudo credentials to run properly - does a lot of apt-get installs.
Leaves behind a few files for pip and for you to investigate.
Uses virtual burrito for python / pip / env management.
