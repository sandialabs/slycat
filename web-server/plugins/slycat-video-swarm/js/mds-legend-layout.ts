/**
 * MDS color-by legend layout shared by the scatterplot gutter and the
 * React overlay, so points and tick labels stay aligned.
 *
 * Gutter matches CCA scatterplot border.right. Right inset matches CCA's
 * final bar x: width - border.left - border.right + 110 = width - 80.
 * Y inset matches CCA border.top / border.bottom.
 */
export const MDS_LEGEND_GUTTER = 150;
export const MDS_LEGEND_RIGHT_INSET = 80;
export const MDS_LEGEND_Y = 40;
