import React, { useState } from "react";

export default function SlycatVariableAliasLabels(props) {

  return (
    <div className={`slycat-variable-alias-labels ${props.uniqueID}`}>
      Placeholder for variable alias ui
    </div>
  );
}